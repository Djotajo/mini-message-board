<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <%- include('navbar') %>

    <div class="messages">
      <% messages.forEach((message) => { %>
      <div class="message">
        <p><%= message.user %></p>
        <p><%= message.text %></p>
        <p>
          <%= message.added.toLocaleTimeString([], { hour: '2-digit', minute:
          '2-digit' }); %> <%= message.added.toLocaleDateString(); %>
        </p>
      </div>

      <% }) %>
    </div>
    <a href="/new"> <button class="newMessageButton">New message</button></a>
    <%- include('footer') %>
  </body>
</html>
